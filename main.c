#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	char code1 = 'A'; // char type variable
	int code2 = 65; //int type variable
  
  
  printf("code1 = %c, (%d)\n", code1, code1);
  printf("code2 = %c, (%d)\n", code2, code2);
  
  system("PAUSE");	
  return 0;
}

// code1 = A, (65)
// code2 = A, (65)
